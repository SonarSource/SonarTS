language: node_js

node_js:
  - "8"

script:
  - yarn build
  - yarn test -- --coverage
  - node scripts/analyse.js
  - yarn ruling
  - yarn lint
  - yarn license-check

cache: yarn

notifications:
  email: false
